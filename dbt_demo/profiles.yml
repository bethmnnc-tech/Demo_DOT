dot_dbt_demo:
  target: databricks
  outputs:
    databricks:
      type: databricks
      method: http
      host: dbc-a5388842-1c80.cloud.databricks.com
      http_path: "{{ env_var('DBT_DATABRICKS_HTTP_PATH', '/sql/1.0/warehouses/269f3f8ac232d6f2') }}"
      token: "{{ env_var('DBT_ACCESS_TOKEN', env_var('DATABRICKS_TOKEN', 'REPLACE_WITH_YOUR_DATABRICKS_TOKEN')) }}"
      catalog: main
      schema: analytics
      threads: 4
